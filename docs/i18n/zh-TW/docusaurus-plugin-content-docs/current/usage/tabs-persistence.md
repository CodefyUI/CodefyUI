---
sidebar_position: 5
title: 分頁與持久化
description: 多分頁工作區、瀏覽器自動儲存，以及把圖匯入／匯出為 JSON。
---

# 分頁與持久化

## 多分頁工作區

CodefyUI 支援以分頁形式呈現多個獨立的畫布。每個分頁都有各自的：

- 節點、連線與排版
- 執行環境與紀錄
- 記錄的輸出與保留的權重（見 [教學檢視器](./teaching-inspector)）
- 復原/重做歷史（最多 50 步）

這讓你可以把多個實驗並排放著 — 例如一個分頁放訓練圖、另一個放推論圖 — 而它們的狀態不會互相干擾。

## 自動儲存

所有分頁都會自動存在瀏覽器裡，因此重新載入頁面時你的工作會被還原。這是瀏覽器本機的儲存；不會同步到伺服器。

儲存使用 **IndexedDB**，每個分頁各存一筆資料。這對大型圖很重要：舊的 `localStorage` 後端對每個來源大約只有 5MB 上限，圖一旦超過就會直接停止儲存。IndexedDB 沒有相當的實務上限，而且只有你剛編輯過的那個分頁會被重寫。

第一次開啟採用 IndexedDB 儲存的版本時，`localStorage` 裡最後存的內容會自動被複製過去 — 你不需要做任何事。舊的 `localStorage` 副本會被保留（所以降回舊版仍然打得開它最後看到的圖），但從此不再更新。

如果你的瀏覽器沒有可用的 IndexedDB — 某些無痕模式，或被沙箱限制的 iframe — 儲存會退回 `localStorage`，也就回到它原本的容量上限與「儲存空間已滿」的警告。

## 匯入／匯出

你可以把任何圖匯出成 JSON 檔案，之後再匯入回來（或分享）：

- **匯出** 會把目前分頁的圖（節點、連線、參數與段落標記）寫成一個 `.json` 檔案。
- **匯入** 會把一個 `.json` 圖載入到新分頁。
- **匯出為 Python** 會產生一個可讀的單檔 Python 程式：每個節點一個函式（參數以可編輯的字面值內嵌）、依執行順序把節點串接起來的流程函式，以及帶有簡易命令列介面的 `main()` 進入點。每個節點函式都委派給畫布所使用的同一份節點實作，因此執行結果與畫布一致。請用相容的 CodefyUI 安裝環境的 Python 來執行；不需要啟動網頁伺服器。用 `--help` 查看裝置、GraphInput JSON、逾時與專案資產等選項。

後端的範例圖使用的正是同一種 JSON 格式，所以匯出的圖也可以用 **[CLI 圖形執行器](./cli-runner)** 在無介面（headless）下執行。

:::tip
因為圖就是純粹的 JSON，它們能乾淨地進行 diff 與版本控制。把圖和你的程式碼一起 commit，即可捕捉一條精確、可重現的管線。
:::
