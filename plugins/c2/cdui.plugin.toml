[plugin]
id              = "c2"
name            = "Chapter 2 — 經典 AI"
version         = "0.1.0"
description     = "Hand-written classifiers for textbook chapter C2: KNN, Linear Regression, Logistic Regression. Same interface as the sklearn-backed production nodes so workflows can swap implementations."
schema_version  = 1
requires_codefyui = ">=0.2.0"

[content]
nodes_dir    = "nodes"
examples_dir = "examples"

[lessons]
chapters = ["C2"]
lessons  = ["C2-2"]
